function twoDimensionArray(num) {
    let arr = [];

    // creating two dimensional array
    for (let i = 0; i < a; i++) {
        for (let j = 0; j < b; j++) {
            arr[i] = [];
        }
    }
}

const x = a;
const y = 3;

const result = twoDimensionArray(x, y);
console.log(result);